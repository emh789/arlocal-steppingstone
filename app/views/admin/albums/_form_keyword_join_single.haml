.arl_form_title
  Add a keyword

- if selectable.does_have_keywords
  = form_with model: [:admin, album], url: admin_album_path(album, pane: :keywords), local: true, class: :arl_form, method: :patch do |f|
    .arl_form_data
      = f.fields_for :album_keywords, album.album_keywords.build do |ak|
        = render 'form_elements/collection_select', form: ak, attribute: :keyword_id, blank: true, collection: selectable.keywords, text_method: :title
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .arl_form_null No keywords.
