.arl_form_title
  Add audio

- if selectable.does_have_audio
  = form_with(model: [:admin, event], url: admin_event_path(event, pane: :audio), local: true, class: :arl_form) do |f|
    .arl_form_data
      = f.fields_for :event_audio, event.event_audio.build do |ea|
        = render 'form_elements/collection_select', form: ea, attribute: :audio_id, blank: true, collection: selectable.audio, label: 'Title', text_method: :full_title
        = render 'form_elements/text_field_sized', form: ea, attribute: :event_order, label: 'Order', size: 4
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .arl_form_null No audio.
