.arl_admin_joined_resource
  %dh.arl_admin_joined_resource_heading= resource_joined_articles_count_statement(resource, punctuated: true)
  - resource.articles_sorted.each do |article|
    %dd.arl_admin_joined_resource_data= article.title
