.arl_admin_joined_resource
  %dh.arl_admin_joined_resource_heading= resource_joined_keywords_count_statement(resource, punctuated: true)
  - resource.keywords_sorted.each do |keyword|
    %dd.arl_admin_joined_resource_data= keyword.title
