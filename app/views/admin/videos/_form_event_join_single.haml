.arl_form_title
  Add event

- if selectable.does_have_events
  = form_with(model: [:admin, video], url: admin_video_path(video, pane: :events), local: true, class: :arl_form) do |f|
    .arl_form_data
      = f.fields_for :event_videos, video.event_videos.build do |ev|
        = render 'form_elements/collection_select', form: ev, attribute: :event_id, blank: true, collection: selectable.events, label: 'Date/venue', text_method: :datetime_and_title
        = render 'form_elements/text_field_sized', form: ev, attribute: :event_order, label: 'Order', size: 4
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .arl_form_null No events.
