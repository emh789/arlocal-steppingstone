.arl_form_title
  Add a link

- if selectable.does_have_links
  = form_with(model: [:admin, infopage], url: admin_infopage_path(infopage, pane: :links), local: true, class: :arl_form) do |f|
    .arl_form_data
      = f.fields_for :infopage_items, f.object.infopage_links.build do |il|
        = il.hidden_field :infopageable_type
        = render 'form_elements/collection_select', form: il, attribute: :infopageable_id, blank: true, collection: selectable.links, label: 'Link', text_method: :title
        = render 'form_elements/select', form: il, attribute: :infopage_group, blank: true, selectable: selectable.item_groups, label: 'Group'
        = render 'form_elements/text_field_sized', form: il, attribute: :infopage_group_order, autocomplete: :off, label: 'Order', size: 4
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .arl_form_null No Links.
