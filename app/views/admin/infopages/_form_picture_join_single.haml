.arl_form_title
  Add picture

- if selectable.does_have_pictures
  = form_with(model: [:admin, infopage], url: admin_infopage_path(infopage, pane: :pictures), local: true, class: :arl_form) do |f|
    .arl_form_data
      = f.fields_for :infopage_items, f.object.infopage_pictures.build do |ip|
        = ip.hidden_field :infopageable_type
        = render 'form_elements/picture_select', form: ip, attribute: :infopageable_id, selectable: selectable
        = render 'form_elements/select', form: ip, attribute: :infopage_group, blank: true, selectable: selectable.item_groups, label: 'Group'
        = render 'form_elements/text_field_sized', form: ip, attribute: :infopage_group_order, autocomplete: :off, label: 'Order', size: 4
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .arl_form_null No pictures.
