.arl_form_title
  Add a picture

- if selectable.does_have_pictures
  = form_with(model: [:admin, keyword], url: admin_keyword_path(keyword, pane: :pictures), local: true, class: :arl_form) do |f|
    .arl_form_data
      = f.fields_for :picture_keywords, keyword.picture_keywords.build do |pk|
        = render 'form_elements/picture_select', form: pk, selectable: selectable
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .arl_form_null No pictures.
