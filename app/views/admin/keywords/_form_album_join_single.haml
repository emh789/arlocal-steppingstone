.arl_form_title
  Add keyword to album

- if selectable.does_have_albums
  = form_with model: [:admin, keyword], url: admin_keyword_path(keyword, pane: :albums), local: true, class: :arl_form do |f|
    .arl_form_data
      = f.fields_for :album_keyword, keyword.album_keywords.build do |ak|
        = render 'form_elements/collection_select', form: ak, attribute: :album_id, blank: true, collection: selectable.albums, label: 'Title', text_method: :title
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .arl_form_null No albums.
